<UserControl x:Class="Stamps.Ui.Pages.TweakDetailPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,12,28,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back link -->
        <TextBlock Grid.Row="0" Margin="0,0,0,4">
            <Hyperlink x:Name="BackLink" Click="OnBackClick">
                ← Back to Tweaks
            </Hyperlink>
        </TextBlock>

        <!-- Tweak description -->
        <TextBlock x:Name="DescriptionText"
                   Grid.Row="1"
                   FontFamily="{DynamicResource Stamps.FontFamily}"
                   FontSize="{DynamicResource Stamps.FontSize.Body}"
                   Foreground="{DynamicResource Stamps.Text.Secondary}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <!-- Tabs -->
        <TabControl x:Name="Tabs"
                    Grid.Row="2"
                    Style="{StaticResource StampsTabControlStyle}"
                    ItemContainerStyle="{StaticResource StampsTabItemStyle}">
            <TabItem x:Name="OverviewTab"   Header="Overview"/>
            <TabItem x:Name="ActionsTab"    Header="Actions"/>
            <TabItem x:Name="SettingsTab"   Header="Settings"/>
        </TabControl>
    </Grid>
</UserControl>
