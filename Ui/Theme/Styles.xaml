<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Sidebar nav item: RadioButton styled as a nav row with left accent on selected -->
    <Style x:Key="SidebarNavItemStyle" TargetType="RadioButton">
        <Setter Property="Height" Value="44"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="GroupName" Value="SidebarNav"/>
        <Setter Property="Foreground" Value="{DynamicResource Stamps.Text.Secondary}"/>
        <Setter Property="FontFamily" Value="{DynamicResource Stamps.FontFamily}"/>
        <Setter Property="FontSize" Value="{DynamicResource Stamps.FontSize.Body}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid Margin="4,2,4,2">
                        <!-- Hover/selected fill -->
                        <Border x:Name="Bg" CornerRadius="6" Background="Transparent"/>
                        <!-- Left accent bar (selected only) -->
                        <Border x:Name="Accent"
                                Width="3" CornerRadius="2"
                                HorizontalAlignment="Left" VerticalAlignment="Center"
                                Height="18" Margin="4,0,0,0"
                                Background="{DynamicResource Stamps.Accent.Primary}"
                                Visibility="Collapsed"/>
                        <!-- Content -->
                        <ContentPresenter Margin="20,0,12,0"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background"
                                    Value="{DynamicResource Stamps.Background.Elevated}"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Stamps.Text.Primary}"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bg" Property="Background"
                                    Value="{DynamicResource Stamps.Background.Elevated}"/>
                            <Setter TargetName="Accent" Property="Visibility" Value="Visible"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Stamps.Text.Primary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- Tab item: centered label, hover highlight, accent bottom border on selected -->
    <Style x:Key="StampsTabItemStyle" TargetType="TabItem">
        <Setter Property="Padding"        Value="20,10"/>
        <Setter Property="FontFamily"     Value="{DynamicResource Stamps.FontFamily}"/>
        <Setter Property="FontSize"       Value="{DynamicResource Stamps.FontSize.Body}"/>
        <Setter Property="Background"     Value="Transparent"/>
        <Setter Property="Foreground"     Value="{DynamicResource Stamps.Text.Secondary}"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
        <Setter Property="BorderBrush"    Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Bg"
                            Cursor="Hand"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          ContentSource="Header"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger SourceName="Bg" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background"
                                    Value="{DynamicResource Stamps.Background.Elevated}"/>
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Stamps.Text.Primary}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Stamps.Text.Primary}"/>
                            <Setter Property="BorderBrush"
                                    Value="{DynamicResource Stamps.Accent.Primary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tab control: no border, tab strip + 1px separator + content -->
    <Style x:Key="StampsTabControlStyle" TargetType="TabControl">
        <Setter Property="Background"      Value="{x:Null}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding"         Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TabPanel Grid.Row="0"
                                  IsItemsHost="True"
                                  Background="Transparent"
                                  HorizontalAlignment="Left"/>
                        <Rectangle Grid.Row="1" Height="1"
                                   Fill="{DynamicResource Stamps.Border.Default}"/>
                        <ContentPresenter Grid.Row="2"
                                          ContentSource="SelectedContent"
                                          Margin="0,16,0,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
